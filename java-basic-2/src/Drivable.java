public interface Drivable {
    void drive(int speed);
    void stop(int withintime);
    int speedUp(int upSpeed);
    int speedDown(int downSpeed);
}

// ここまでで一つのファイル
// ーー


// ーーー
// k
// myCar.drive();